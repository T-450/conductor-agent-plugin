name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  validate:
    name: Validate Manifests & Skills
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ['3.10', '3.11', '3.12']
        node-version: ['18', '20']

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Run Manifest & Skill Verification
        run: python evals/run_evals.py

      - name: Run Eval-Driven Development (EDD) Suite
        run: python evals/edd.py run --trials 3

      - name: Run Regression Gate Check
        run: python evals/edd.py regression --baseline evals/baselines/baseline_v1.2.0.json

      - name: Test CLI Bridge
        run: node bin/conductor help
